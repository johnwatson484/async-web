const routes = [].concat(
  require('../routes/home'),
  require('../routes/result'),
  require('../routes/public')
)

module.exports = {
  plugin: {
    name: 'router',
    register: (server, options) => {
      server.route(routes)
    }
  }
}
